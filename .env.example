# Origen del backend NestJS (sin /api/v1; el cliente lo añade automáticamente)

# Desarrollo local
VITE_API_URL=http://localhost:3000

# Producción (CI en GitHub Actions; no commitear .env con este valor)
# VITE_API_URL=http://18.223.229.175
